import { FunctionComponent } from "react";

const Logout: FunctionComponent = () => {
  return (
    <div className="admin">
      <p className="text-center fs-5 fw-bold">Protected resources</p>
    </div>
  );
};
export default Logout;
